# security.txt for Bearup International Ministries
# Edit Contact, Policy, and Acknowledgments URLs to match this site's
# canonical URL (src/lib/site.config.ts -> siteConfig.url) before deploying.
#
# Expiration MUST be a future ISO 8601 datetime. Bump it on a regular cadence
# (the included GitHub Action `security-txt-expiry.yml` opens an issue when
# the date gets close).
#
# NOTE: Two Canonical lines are listed because GitHub Pages does not serve
# files in dot-prefixed directories (`.well-known/`). The same content is
# also published at /security.txt as a fallback. Hosts that support
# `.well-known/` (Cloudflare Pages, Netlify, plain nginx, etc.) serve both.
# The drift checker enforces that /security.txt stays in sync with this file.

Contact: mailto:Info@bearupinternationalministries.org
Expires: 2027-01-01T00:00:00.000Z
Preferred-Languages: en
Canonical: https://bearupinternationalministries.org/.well-known/security.txt
Canonical: https://bearupinternationalministries.org/security.txt
Policy: https://bearupinternationalministries.org/vulnerability-disclosure-policy
Acknowledgments: https://bearupinternationalministries.org/security-acknowledgements
